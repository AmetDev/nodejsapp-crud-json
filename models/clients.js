class Client {
	constructor(codeClient, nameClient, codeRequest, phone) {
		this.codeClient = codeClient
		this.nameClient = nameClient
		this.codeRequest = codeRequest
		this.phone = phone
	}
}

module.exports = Client
